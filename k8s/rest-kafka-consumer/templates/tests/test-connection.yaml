apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rest-kafka-consumer.fullname" . }}-test-connection"
  labels:
    {{- include "rest-kafka-consumer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rest-kafka-consumer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
